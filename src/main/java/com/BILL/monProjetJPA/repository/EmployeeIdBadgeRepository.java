package com.BILL.monProjetJPA.repository;

import com.BILL.monProjetJPA.model.EmployeeIdBadge;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmployeeIdBadgeRepository extends JpaRepository<EmployeeIdBadge, Long> {
}
